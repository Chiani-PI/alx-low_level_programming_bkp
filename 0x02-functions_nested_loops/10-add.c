#include "main.h"
/**
 * add - A function that adds two integers and returns the result,
 * @a: first int in addition
 * @b: second int in addition
 * Return: sum of a and b
 */
int add(int a, int b)
{
int c;
c = a + b;
return (c);
}
